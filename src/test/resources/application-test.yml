spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  datasource:
    # Используем Testcontainers через специальный JDBC-URL
    url: jdbc:tc:postgresql:15-alpine:///orm_platform_test
    username: postgres
    password: postgres

